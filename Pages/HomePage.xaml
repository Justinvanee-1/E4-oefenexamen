<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="E4_Project.Pages.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:E4_Project.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="40" Background="#f0f4f8">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="25">

            <!-- Titel -->
            <TextBlock Text="Dreamscape"
                   HorizontalAlignment="Center"
                   FontSize="48"
                   FontWeight="Bold"
                   Foreground="Blue"/>

            <!-- Subtitel -->
            <TextBlock Text="Inventory beheren"
                   HorizontalAlignment="Center"
                   FontSize="24"
                   FontWeight="SemiBold"
                   Foreground="#34495e"/>


            <!-- Knoppen -->
            <StackPanel Spacing="15" HorizontalAlignment="Center">

                <!-- Inventory knop -->
                <Button Content="Inventory"
                    Click="Inventory_Click"
                    Width="200"
                    Height="50"
                    Background="Green"
                    Foreground="White"
                    FontSize="18"
                    FontWeight="Bold"
                    CornerRadius="10"/>

                <TextBlock Text="Bezoek market"     
                    HorizontalAlignment="Center"
                    FontSize="24"
                    FontWeight="SemiBold"
                    Foreground="#34495e"/>      

                <!-- Market knop -->
                <Button Content="Market"
                    Click="Market_Click"
                    Width="200"
                    Height="50"
                    Background="Red"
                    Foreground="White"
                    FontSize="18"
                    FontWeight="Bold"
                    CornerRadius="10"/>

            </StackPanel>

        </StackPanel>
    </Grid>
</Page>
